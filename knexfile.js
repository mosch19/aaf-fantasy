// const knex = require('knex')(require('./knexfile'));

module.exports = {
    client: 'mysql',
    connection: {
      user: 'root',
      password: 'password',
      database: 'aaf_user_database'
    }
  }